@model IEnumerable<WebTruyen.Models.TacGia>

@{
    ViewBag.Title = "DSTacGia";
    Layout = "~/Views/Shared/_LayoutAdminWeb.cshtml";
}

<h2>DSTacGia</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Mã Tác Giả
        </th>
        <th>
            Mã Tài Khoản
        </th>
        <th>
            Bút danh
        </th>
        <th>
            Ngày Đăng ký
        </th>
        <th>
            Vai trò
        </th>
        <th>
            Xét duyệt
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaTG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaTK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ButDanh)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayDangKy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VaiTro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DaDuyet)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id= item.MaTG}) |
            @Html.ActionLink("Details", "Details", new { id = item.MaTG}) |
            @Html.ActionLink("Delete", "Delete", new { id= item.MaTG })
        </td>
    </tr>
}

</table>
